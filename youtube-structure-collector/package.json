{
  "name": "youtube-structure-collector",
  "version": "2.0.0",
  "private": true,
  "description": "Turn YouTube transcript sentences into reusable fill-in-the-blank patterns.",
  "scripts": {
    "build": "bash scripts/build.sh",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/lib/structure-utils.js && node --check src/content/content-script.js && node --check src/popup/popup.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
